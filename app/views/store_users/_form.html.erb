<%= form_with(model: store_user, local: true) do |form| %>
  <% if store_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store_user.errors.count, "error") %> prohibited this store_user from being saved:</h2>

      <ul>
      <% store_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :store_id %>
    <%= form.number_field :store_id, id: :store_user_store_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :store_user_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
